apiVersion: v1
kind: Service
metadata:
  name: parklense-vehicle-backend-service
  labels:
    app: parklense-vehicle-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: parklense-vehicle-backend 